version: '3.8'

services:
  article-generator:
    build: .
    container_name: gemini-article-generator
    ports:
      - "3001:3001"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3001
