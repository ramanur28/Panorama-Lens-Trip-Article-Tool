services:
  article-generator:
    build: .
    container_name: gemini-article-generator
    ports:
      - "3001:3001"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3001
      - WP_USERNAME=ramanur321@gmail.com
      - WP_APPLICATION_PASSWORD="CJDa AiNi wvUS TK7l 8QrD zeTV"
    volumes:
      - ./data:/app/data
